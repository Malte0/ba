import type { CARD } from "../../types";

const card: CARD = {
    index: 28,
    color: "red",
    night: true,
    symbols: ["Stein"],
    points: 3,
    multiplier: "Schimaere"
} 

export default card;
