import type { CARD } from "../../types";

const card: CARD = {
    index: 54,
    color: "green",
    symbols: ["Schimaere"],
    condition: ["Distel", "Distel"],
    points: 4,
    multiplier: "map"
} 

export default card;
