import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 68,
    color: "blue",
    condition: ["Stein", "Stein", "Stein", "Stein", "Stein"],
    points: 24
} 

export default card;
