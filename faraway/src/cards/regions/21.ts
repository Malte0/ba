import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 21,
    color: "blue",
    night: true,
    condition: ["Stein", "Stein"],
    points: 8
} 

export default card;
