import type { CARD } from "../../types";

const card: CARD = {
    index: 30,
    color: "red",
    night: true,
    symbols: ["Stein"],
    points: 2,
    multiplier: "Stein"
} 

export default card;
