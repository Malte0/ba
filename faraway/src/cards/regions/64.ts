import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 64,
    color: "blue",
    map: true,
    condition: ["Stein", "Stein", "Distel", "Distel"],
    points: 18
} 

export default card;
