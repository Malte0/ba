import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 37,
    color: "yellow",
    night: true,
    condition: ["Distel"],
    points: 3,
    multiplier: "night"
} 

export default card;
