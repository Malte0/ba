import type { CARD } from "../../types";

const card: CARD = {
    index: 37,
    color: "yellow",
    night: true,
    condition: ["Distel"],
    points: 3,
    multiplier: "night"
} 

export default card;
