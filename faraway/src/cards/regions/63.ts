import type { CARD } from "../../types";

const card: CARD = {
    index: 63,
    color: "green",
    map: true,
    condition: ["Schimaere", "Schimaere", "Distel"],
    points: 15
} 

export default card;
