import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 63,
    color: "green",
    map: true,
    condition: ["Schimaere", "Schimaere", "Distel"],
    points: 15
} 

export default card;
