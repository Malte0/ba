import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 66,
    color: "blue",
    condition: ["Stein", "Stein", "Stein", "Stein"],
    points: 20
} 

export default card;
