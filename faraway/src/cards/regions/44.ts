import type { CARD } from "../../types";

const card: CARD = {
    index: 44,
    color: "yellow",
    condition: ["Stein", "Distel"],
    points: 2,
    multiplier: ["yellow", "blue"]
} 

export default card;
