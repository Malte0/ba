import type { CARD } from "../../types";

const card: CARD = {
    index: 47,
    color: "yellow",
    condition: ["Distel", "Schimaere"],
    points: 2,
    multiplier: ["yellow", "red"]
} 

export default card;
