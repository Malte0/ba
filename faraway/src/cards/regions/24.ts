import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 24,
    color: "blue",
    night: true,
    symbols: ["Stein"],
    condition: ["Schimaere"],
    points: 2,
    multiplier: "night"
} 

export default card;
