import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 55,
    color:"blue",
    map: true,
    symbols: ["Stein"],
    condition: ["Schimaere", "Distel", "Distel"],
    points: 3,
    multiplier: "Stein"
} 

export default card;
