import type { CARD } from "../../types";

const card: CARD = {
    index: 55,
    color:"blue",
    map: true,
    symbols: ["Stein"],
    condition: ["Schimaere", "Distel", "Distel"],
    points: 3,
    multiplier: "Stein"
} 

export default card;
