import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 25,
    color: 'yellow',
    night: true,
    points: 1,
    multiplier: ["green", "yellow"]
} 

export default card;
