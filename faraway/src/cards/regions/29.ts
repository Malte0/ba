import type { CARD } from "../../types";

const card: CARD = {
    index: 29,
    color: "yellow",
    night: true,
    symbols: ["Distel"],
    points: 2,
    multiplier: "Distel"
} 

export default card;
