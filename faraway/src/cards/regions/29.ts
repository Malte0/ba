import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 29,
    color: "yellow",
    night: true,
    symbols: ["Distel"],
    points: 2,
    multiplier: "Distel"
} 

export default card;
