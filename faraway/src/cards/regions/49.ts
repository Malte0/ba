import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 49,
    color: "blue",
    map: true,
    condition: ["Stein", "Stein", "Distel"],
    points: 12
} 

export default card;
