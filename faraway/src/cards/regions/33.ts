import type { CARD } from "../../types";

const card: CARD = {
    index: 33,
    color: "yellow",
    night: true,
    map: true,
    symbols: [],
    points: 3,
    multiplier: "Distel"
} 

export default card;
