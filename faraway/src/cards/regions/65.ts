import type { CARD } from "../../types";

const card: CARD = {
    index: 65,
    color: "yellow",
    map: true,
    condition: ["Distel", "Distel", "Distel"],
    points: 3,
    multiplier: ["yellow", "green"]
} 

export default card;