import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 48,
    color: "red",
    symbols: ["Schimaere"],
    points: 3,
    multiplier: "Stein"
} 

export default card;
