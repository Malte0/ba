import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 43,
    color: "blue",
    symbols: ["Stein"],
    points: 10,
    multiplier: "color-set"
} 

export default card;
