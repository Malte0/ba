import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 31,
    color: "yellow",
    night: true,
    points: 1,
    multiplier: ["yellow", "red"]
} 

export default card;
