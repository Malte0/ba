import type { CARD } from "../../types";

const card: CARD = {
    index: 62,
    color: 'yellow',
    map: true,
    condition: ["Distel", "Distel", "Distel"],
    points: 3,
    multiplier: ["yellow", "blue"]
} 

export default card;
