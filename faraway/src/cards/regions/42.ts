import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 42,
    color: "yellow",
    condition: ["Stein", "Schimaere"],
    points: 2,
    multiplier: ["yellow", "green"]
} 

export default card;
