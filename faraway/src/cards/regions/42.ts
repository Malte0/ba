import type { CARD } from "../../types";

const card: CARD = {
    index: 42,
    color: "yellow",
    condition: ["Stein", "Schimaere"],
    points: 2,
    multiplier: ["yellow", "green"]
} 

export default card;
