import type { CARD } from "../../types";

const card: CARD = {
    index: 56,
    color:"yellow",
    symbols: ["Distel"],
    condition: ["Schimaere", "Schimaere", "Stein"],
    points: 4,
    multiplier: ["blue"]
} 

export default card;
