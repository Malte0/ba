import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 56,
    color:"yellow",
    symbols: ["Distel"],
    condition: ["Schimaere", "Schimaere", "Stein"],
    points: 4,
    multiplier: ["blue"]
} 

export default card;
