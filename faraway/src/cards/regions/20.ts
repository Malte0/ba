import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 20,
    color: "green",
    night: true,
    map: true,
    condition: ["Stein"],
    points: 2,
    multiplier: "night"
} 

export default card;
