import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 27,
    color: "yellow",
    night: true,
    points: 1,
    multiplier: ["yellow", "blue"]
} 

export default card;
