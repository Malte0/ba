import type { CARD } from "../../types";

const card: CARD = {
    index: 60,
    color: "blue",
    map: true,
    condition: ["Stein", "Stein", "Schimaere", "Schimaere"],
    points: 16
} 

export default card;
