import type { CARD } from "../../types";

const card: CARD = {
    index: 50,
    color: "yellow",
    symbols: ["Stein"],
    condition: ["Distel", "Distel"],
    points: 4,
    multiplier: ["green"]
} 

export default card;
