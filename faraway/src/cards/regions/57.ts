import type { CARD } from "../../types";

const card: CARD = {
    index: 57,
    color: "red",
    condition: ["Distel", "Distel", "Distel"],
    points: 4,
    multiplier: "Stein"
} 

export default card;
