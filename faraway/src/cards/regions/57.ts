import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 57,
    color: "red",
    condition: ["Distel", "Distel", "Distel"],
    points: 4,
    multiplier: "Stein"
} 

export default card;
