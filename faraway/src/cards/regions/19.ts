import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 19,
    color: "red",
    symbols: ["Distel"],
    points: 2,
    multiplier: "Distel"
} 

export default card;
