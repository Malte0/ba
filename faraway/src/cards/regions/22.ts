import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 22,
    color: "green",
    night: true,
    map: true,
    points: 1,
    multiplier: "map"
} 

export default card;
