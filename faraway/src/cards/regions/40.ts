import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 40,
    color: "blue",
    night: true,
    condition: ["Distel", "Schimaere", "Stein"],
    points: 3,
    multiplier: "night"
} 

export default card;
