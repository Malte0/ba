import type { CARD } from "../../types";

const card: CARD = {
    index: 40,
    color: "blue",
    night: true,
    condition: ["Distel", "Schimaere", "Stein"],
    points: 3,
    multiplier: "night"
} 

export default card;
