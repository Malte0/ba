import type { CARD } from "../../types";

const card: CARD = {
    index: 46,
    color: "blue",
    map: true,
    condition: ["Stein", "Stein", "Schimaere"],
    points: 10
} 

export default card;
