import type { REGION_CARD } from "../../types";

const card: REGION_CARD = {
    index: 53,
    color: "yellow",
    symbols: ["Schimaere"],
    condition: ["Distel", "Distel"],
    points: 4,
    multiplier: ["red"]
} 

export default card;
